const { fastHTML, fastHTMLCustom } = require('./fastHTML');
const { showHTML, showHTMLCustom } = require('./showHTML');

module.exports = {
  fastHTML,
  fastHTMLCustom,
  showHTML,
  showHTMLCustom,
};
